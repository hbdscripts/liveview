{
  "name": "live-visitors",
  "version": "1.0.1",
  "private": true,
  "description": "Private Shopify app: Live Visitors admin dashboard",
  "scripts": {
    "dev": "node server/index.js",
    "start": "NODE_ENV=production node scripts/prune-runtime-files.js && NODE_ENV=production node server/index.js",
    "test": "node --test",
    "migrate": "node server/migrate.js",
    "backup:daily": "node server/runDailyBackup.js",
    "generate-ingest-secret": "node scripts/generate-ingest-secret.js",
    "deploy": "node scripts/shopify-deploy.js",
    "deploy:interactive": "shopify app deploy",
    "config:link": "node scripts/shopify-link.js",
    "config:link:interactive": "shopify app config link",
    "shopify:info": "shopify app info"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.990.0",
    "@sentry/node": "^10.38.0",
    "@shopify/web-pixels-extension": "^2.16.0",
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "geoip-lite": "^1.4.9",
    "multer": "^2.0.2",
    "openai": "^6.22.0",
    "pg": "^8.13.0"
  },
  "author": "",
  "license": "UNLICENSED"
}
