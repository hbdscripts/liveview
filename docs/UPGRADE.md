# Upgrade page

The **Upgrade** page (`/upgrade`, `upgrade.html`) is a static marketing placeholder. The dashboard overlay loader is intentionally **not** shown on this page so the static content loads without a spinner.

Future work: replace with a real upgrade/plan comparison or remove the route.
